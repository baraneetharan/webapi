version: 1.0.{build}
os: Visual Studio 2015
before_build:
- cmd: >-
    dnvm upgrade
    dnvm upgrade -r coreclr
    dnu restore
build:
  publish_wap: true
  publish_nuget: true
  publish_nuget_symbols: true
  verbosity: minimal
